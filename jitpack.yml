jdk:
    - openjdk17
env:
    JITPACK: "true"
install:
    # We use -Psigning.required=false to tell the plugin not to complain.
    # We use assemble + publishToMavenLocal without naming specific sign tasks.
    - ./gradlew assemble publishToMavenLocal -Psigning.required=false -x test
